# Noms des secrets Supabase (Edge Functions → Secrets). JAMAIS de valeur ici.
ringover=
jarvi=
modjo=
anthropic=
ringover_webhook=
slack_webhook=
cron_token=
# Pour `supabase functions serve` en local uniquement, copier ce fichier en .env (ignoré par git).
